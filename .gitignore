/webview
/netinstall/dist
/_site
/test/lib/__pycache__
/test/report/__pycache__
/test/apparatus/__pycache__

# Every built artifact, the app folder each one makes beside itself, and the
# build cache. One rule, because they are all in one room now -- this was
# fifty-four lines, one pair per artifact, kept by hand and checked by nothing.
/test/out/
